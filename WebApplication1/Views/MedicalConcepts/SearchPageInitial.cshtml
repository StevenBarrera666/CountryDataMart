@using System.Data
@model DataTable

<link href="~/Scripts/tables/jquery.dynatable.css" rel="stylesheet" />
<script type="text/javascript" src="~/Scripts/tables/jquery.dynatable.js"></script>
<style>
    .dynatable-per-page-select {
        padding: 0px !important;
    }

    #modalCurriculum embed {
        width: 100%;
        height: 80vh;
    }
</style>
<table class="ui selectable celled table ui center aligned tablesInitial" id="tableSearchPageInitial">
    <thead>
        <tr>
            @foreach (System.Data.DataColumn col in Model.Columns)
            {
                if (col.ColumnName != "Ruta")
                {
                    <th>@col.ColumnName</th>
                }
            }
        </tr>
    </thead>
    <tbody>
        @foreach (System.Data.DataRow row in Model.Rows)
        {
            <tr>
                @foreach (DataColumn col in Model.Columns)
                {
                    if (col.ColumnName != "Ruta")
                    {
                        if (col.ColumnName == "Archivo")
                        {
                            <td><a href="/@row["Ruta"]" target="_blank">@row[col.ColumnName]</a> </td>
                        }
                        else
                        {
                            <td>@row[col.ColumnName]</td>
                        }

                    }
                }
            </tr>
        }
    </tbody>
</table>

<script type="text/javascript">

    var processingComplete = function () {
        $.each($('#tableSearchPageInitial tr'), function (index, value) {
            var classElement = $(this).find('td:eq(6)').html();
            classElement = classElement == "Rojo" ? "red" : classElement == "Amarillo" ? "yellow" : classElement == "Verde" ? "green" : "";
            $(this).find('td:eq(6)').addClass(classElement);

            if (classElement == "red")
                $(this).find('td:eq(6)').html('Vencido');
            if (classElement == "yellow")
                $(this).find('td:eq(6)').html('Por Vencer');
            if (classElement == "green")
                $(this).find('td:eq(6)').html('Vigente');

            var classElement1 = $(this).find('td:eq(7)').html();
            classElement1 = classElement1 == "No apto" ? "red" : classElement1 == "Aplazado" ? "yellow" : classElement1 == "Apto" ? "green" : "";
            $(this).find('td:eq(7)').addClass(classElement1);



            var vigencia = classElement;
            var concepto = $(this).find('td:eq(7)').html();
            var habilitado;

            if (vigencia == "red") {
                habilitado = "No";
            }
            
            if (vigencia == "yellow") {
                if (concepto == "Apto") {
                    habilitado = "Si";
                }
            }
            
            if (vigencia == "yellow") {
                if (concepto == "No apto") {
                    habilitado = "No";
                }
            }
            
            if (vigencia == "green") {
                if (concepto == "Apto") {
                    habilitado = "Si";
                }
            }
            
            if (vigencia == "green") {
                if (concepto == "No apto") {
                    habilitado = "No";
                }
            }
            
            if (habilitado == "Si") {
                $(this).find('td:eq(8)').addClass("green");
                $(this).find('td:eq(8)').html('Si <i class="fa fa-thumbs-o-up" aria-hidden="true"></i>');
            }
            else {
                $(this).find('td:eq(8)').addClass("red");
                $(this).find('td:eq(8)').html('No <i class="fa fa-thumbs-o-down" aria-hidden="true"></i>');
            }

        });
    }
    $('#tableSearchPageInitial').dynatable({
    }).bind('dynatable:afterProcess', processingComplete);
    processingComplete();
</script>