@using WebApplication1.Models;
@{
    var session = Session["SessionUser"] as SessionModels;
}
@{ ViewBag.Title = "LoginFacade"; Layout = "~/Views/Shared/_LayoutLoginFacade.cshtml"; } @*@using (Ajax.BeginForm("ValidateLogin", "Account", null, new AjaxOptions() { OnBegin = "OnBeginLogin()", OnSuccess = "OnSuccessLogin(data)", OnFailure = "OnError(xhr)", HttpMethod = "POST" }, new { @id = "formLogin" }))*@








<div class="dropdown dropdown-roles">
    <button class="btn dropdown-toggle dropdown-roles__button"
            type="button"
            id="dropdownMenu2"
            data-toggle="dropdown"
            aria-haspopup="true"
            aria-expanded="false"
             style="color:blue">
        Seleccione un perfil
    </button>


    <div class="dropdown-menu dropdown-roles__menu"
         aria-labelledby="dropdownMenu2">

        <button class="dropdown-item"
                type="button"
                onclick="alturas()">
            Administrador
        </button>

        <button class="dropdown-item" type="button">
            Proveedor
        </button>

        <button class="dropdown-item" type="button">
            Cliente
        </button>
    </div>
</div>